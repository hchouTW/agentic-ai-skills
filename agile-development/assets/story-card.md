# Story Card

## Story

As a `<actor>`, I want `<capability>`, so that `<outcome>`.

## Acceptance Criteria

- [ ] Given `<context>`, when `<action>`, then `<observable result>`.
- [ ] Given `<failure or boundary context>`, when `<action>`, then `<safe or useful result>`.
- [ ] Existing behavior remains compatible for `<affected users/interfaces>`.

## Assumptions

- `<material assumption>`

## Scope

In:
- `<included behavior>`

Out:
- `<deferred behavior>`

## Validation

- [ ] `<focused test or check>`
- [ ] `<broader check if warranted>`

## Risks

- `<risk>` - mitigated by `<test, design choice, rollout, or follow-up>`.
